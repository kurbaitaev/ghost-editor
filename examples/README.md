# Worked examples

Real `reel.json` files from shipped reels. Copy the closest one into a new
project and rewrite the takes and beats against the new transcript. Every
time is an ORIGINAL-recording second of that project's own recording, so the
times themselves don't transfer. The structure and the choices do.

| file | recording | style | what it shows |
|---|---|---|---|
| `genz-motion-en.reel.json` | one clean English take, wide framing | motion (recreated from a reference edit) | `autocut` takes, editorial captions with pill tags and highlight, all 5 scene kinds, music bed, push-in whooshes |
| `gameplan-motion-ru.reel.json` | Russian, already edited, captions burned in | motion | `captions.onlyInScenes`, scenes over about half the runtime, Cyrillic text in scenes, hero `size`, AI B-roll |
| `gameplan-memes-ru.reel.json` | the same recording | restrained + memes | `hold` beats for memes on a tight edit, compact cards under burned-in captions, `captions: none` |

To render one yourself you need its recording. These files are for reading and
copying, not for rendering as they are.
